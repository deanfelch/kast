<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Kast - View Conversation</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css" />
<link rel="stylesheet" href="/css/frontend.css" />
</head>
<body class="bg-dark text-light">
  <div class="container py-5">
    <div class="conversation-header">
      <h2 class="mb-0">🎧 Conversation <%= conversationId %></h2>
      <h2><a href="/record?conversationId=<%= conversationId %>" class="btn btn-primary">
        ➕ New Recording
      </a></h2>
    </div>
    <div class="recording-list">
    <% if (kasts.length === 0) { %>
      <div class="alert alert-secondary text-center">
        No recordings yet. Click “New Recording” to add one.
      </div>
    <% } else { %>
        <% kasts.forEach(kast => { %>
        <div class="recording-row d-flex flex-column flex-md-row justify-content-between align-items-md-center">
            <div class="mb-2 mb-md-0">
                <strong><%= new Date(kast.uploaded_at).toLocaleDateString() %> @ <%= new Date(kast.uploaded_at).toLocaleTimeString() %></strong> — 
                <div class="kast-title-container d-flex align-items-center gap-2">
                    <span
                        class="kast-title"
                        data-id="<%= kast.id %>"
                        contenteditable="false"
                        title="Click pencil to edit title"
                    >
                        <%= kast.title || "Untitled Kast" %>
                    </span>

                    <button class="edit-icon btn btn-sm btn-outline-light" data-id="<%= kast.id %>">
                        ✏️
                    </button>

                    <span class="save-confirmation text-success fw-bold" style="opacity: 0;">✅</span>

                    <span class="kast-username ms-2 text-muted small">
                        by <%= kast.username || "Unknown" %>
                    </span>
                </div>
            </div>
            <div class="mb-2 mb-md-0">
              <span class="kast-username">by <%= kast.username || "Unknown" %></span>
            </div>
            <div>
              <audio controls preload="metadata" class="mt-2 mt-md-0" style="height: 32px; max-width: 300px;">
                <source src="https://gateway.pinata.cloud/ipfs/<%= kast.cid %>" type="audio/webm">
                Your browser does not support the audio tag.
              </audio>
            </div>
          </div>
        <% }) %>
    <% } %>
    </div>
  </div>

  <script>
    document.querySelectorAll(".edit-icon").forEach(button => {
        button.addEventListener("click", () => {
        const id = button.dataset.id;
        const container = button.closest(".kast-title-container");
        const titleEl = container.querySelector(".kast-title");
        const confirmation = container.querySelector(".save-confirmation");

        const isEditing = titleEl.isContentEditable;

        if (!isEditing) {
            // Enable editing
            titleEl.contentEditable = "true";
            titleEl.classList.add("editing");
            titleEl.focus();
            placeCursorAtEnd(titleEl);
            button.textContent = "💾"; // switch to save icon
        } else {
            // Save and disable editing
            const newTitle = titleEl.innerText.trim();
            titleEl.contentEditable = "false";
            titleEl.classList.remove("editing");
            button.textContent = "✏️";

            // Save to server
            fetch(`/kasts/${id}/title`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ title: newTitle })
            })
            .then(res => res.json())
            .then(data => {
            if (data.success) {
                confirmation.classList.add("show");
                setTimeout(() => confirmation.classList.remove("show"), 1200);
            } else {
                alert("⚠️ Failed to save title.");
            }
            })
            .catch(err => {
            console.error("Save error:", err);
            alert("❌ Could not save title.");
            });
        }
        });
    });

    function placeCursorAtEnd(el) {
        const range = document.createRange();
        const sel = window.getSelection();
        range.selectNodeContents(el);
        range.collapse(false);
        sel.removeAllRanges();
        sel.addRange(range);
    }
    </script>

</body>
</html>