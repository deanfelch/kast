<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Kast - View Conversation</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css" />
<link rel="stylesheet" href="/css/frontend.css" />
</head>
<body class="bg-dark text-light">
  <div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h2 class="mb-0">🎧 Conversation <%= conversationId %></h2>
      <a href="/record?conversationId=<%= conversationId %>" class="btn btn-primary">
        ➕ New Recording
      </a>
    </div>

    <% if (kasts.length === 0) { %>
      <div class="alert alert-secondary text-center">
        No recordings yet. Click “New Recording” to add one.
      </div>
    <% } else { %>
      <div class="list-group bg-dark">
        <% kasts.forEach(kast => { %>
          <div class="kast-item d-flex justify-content-between align-items-center">
            <div>
              <small class="text-muted">
                <%= new Date(kast.uploaded_at.replace(" ", "T")).toLocaleString() %>
              </small>
            </div>
            <audio controls>
              <source src="https://gateway.pinata.cloud/ipfs/<%= kast.cid %>" type="audio/webm">
              Your browser does not support the audio tag.
            </audio>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>
</body>
</html>